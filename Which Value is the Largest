#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main(){
    int x, y;
    printf("Enter the Value of x and y: ");
    scanf("%d%d",&x,&y);
    (x>y) ? printf("X is Largest") : printf("Y is Largest");
    return 0;
}
