#include <stdio.h>
int main(){
    int x, y;

    printf("Enter X = ");
    scanf("%d", &x);

    printf("Enter Y = ");
    scanf("%d", &y);

    if((x>y))
    printf("%d is Large", x);
    else
    printf("%d is Large", y);
    return 0;
}
